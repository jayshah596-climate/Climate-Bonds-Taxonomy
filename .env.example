# Canonical site URL, used in metadata, sitemap.xml and JSON-LD structured data.
NEXT_PUBLIC_SITE_URL=https://climate-bonds-taxonomy-explorer.vercel.app

# Optional. If set, the AI Assistant (/api/assistant) grounds a real Claude call
# in the retrieved knowledge-base context instead of falling back to local
# retrieval-only mode. Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
